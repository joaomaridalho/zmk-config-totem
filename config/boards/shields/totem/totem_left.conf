# Increase debounce to reduce ghost/repeated keypresses
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=8
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=8

# Boost TX power to maintain stable BLE connection to dongle
# (prevents stuck keys caused by dropped connection mid-keypress)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
